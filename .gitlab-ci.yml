stages:
  - deploy

deploy:
  stage: deploy
  image: ${BUILDER_PATH} 
  tags:
    - docker
  script: 
    - sed 's/vX.X.X/{CI_COMMIT_TAG}/g' setup.py
    - python3 setup.py sdist bdist_wheel
    - python -m twine upload dist/*
  only:
    - /v.*/

