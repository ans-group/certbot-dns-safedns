# Oldest supported versions of our dependencies, per pyproject.toml.
# Used by CI to verify compatibility with the floor of our supported range.
# pyopenssl/josepy/cryptography are pinned to versions contemporary with
# acme/certbot 2.7.0 to avoid unrelated resolver drift (newer josepy/pyopenssl
# releases remove APIs that acme/certbot 2.7.0 still relies on).
acme==2.7.0
certbot==2.7.0
dns-lexicon==3.14.0
josepy<2.0
pyopenssl<27
